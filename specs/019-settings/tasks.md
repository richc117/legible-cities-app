# Tasks: Settings

- [x] T001 The settings shape, its defensive reader and the pure formatting in `src/shared/settings.ts`.
- [x] T002 The store over `<userData>/settings.json`, the bounded folder walk, the reset and its own guard.
- [x] T003 The stored tier in `resolveConfig`, with `settings` as a source.
- [x] T004 The service, the two folder choosers, the handlers and the picked-path guard; `PickedPaths` moved to its own module.
- [x] T005 `Exporter` reads the export folder per export; `Exporter.live` and a new `Sidecar.inFlight` for the reset's gate.
- [x] T006 The bridge: `api.settings` in the contract, the preload and the channels.
- [x] T007 The Settings screen, the theme module, the route from the header, the styles.
- [x] T008 Unit tests: the reader, the store, the walk, the reset guard, the refusal of a path no dialog answered, the handlers.
- [x] T009 The end-to-end scenarios, including a theme and a folder across a relaunch.
- [x] T010 ARCHITECTURE, DESIGN 8.2, CLAUDE.md, the bridge contract.
- [ ] T011 Checks, scanners, the reviewer; the pull request.
