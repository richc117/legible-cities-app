# Tasks: The diagnostics panel

- [x] T001 `RunReport` on the run's snapshot, bound in `#draw`, cleared at `#begin`.
- [x] T002 The formatting helpers in `engine/diagnostics.ts`: the percentage, the methods, the rows, the copy block.
- [x] T003 The panel: caveats, score, the table with its explanations, "Copy as text"; styles and the design rule.
- [x] T004 `api.clipboard.write`: the channel, the validating handler, the preload method.
- [x] T005 The stand-in's `caveats` and `issues`, and the control keys for them.
- [x] T006 Unit, real-engine and end-to-end tests; the documents.
- [ ] T007 Checks, scanners, the reviewer; the pull request.
