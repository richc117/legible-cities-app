# Tasks: Notice a layout laid out again from another project

- [x] T001 `made` on the record and in `LayoutDone`; `relaid` in `LayoutResult`; the store and the bridge's main side.
- [x] T002 The run carries `relaid`; the sentence; the time beside the id.
- [x] T003 The stand-in keeps `made` per layout and rewrites it on `force`.
- [x] T004 Unit, real-engine and end-to-end tests; the contracts and the architecture page.
- [ ] T005 Checks, scanners, the reviewer; the pull request.
