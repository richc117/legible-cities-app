# Tasks: The Library adds and removes feeds

- [x] T001 Pin v0.7.0 and regenerate the types.
- [x] T002 `feeds.pickZip` on the bridge; the main-side dialog and the remembered paths; the engine bridge's guard for `feeds.add` and `feeds.remove`.
- [x] T003 `FeedAdd` run: `feeds.add` with its two stages, cancel, the engine's sentence.
- [x] T004 The Library's feed list, the Add feed dialog, the create dialog's select with the typed fallback, the empty state's action.
- [x] T005 The stand-in's `feeds.list`, `feeds.add`, `feeds.remove`.
- [x] T006 Unit tests (guard, dialog handler, run, dialog field rules), the real-engine list, the end-to-end scenarios.
- [x] T007 ARCHITECTURE, CLAUDE.md, the bridge contract.
- [x] T008 Checks, scanners, the reviewer; the pull request.
