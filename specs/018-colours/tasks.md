# Tasks: Line colours, the feed's and a person's

- [x] T001 `react-colorful` as a devDependency, with its licence in `THIRD_PARTY_NOTICES.md`.
- [x] T002 `Palette`, `isHexColor` and `validatePalette` on the record; `completeColors` in the API and the preload.
- [x] T003 The store writes the palette and refuses a project with no layout; the bridge's main side checks the shape first.
- [x] T004 Every `map.build` carries the palette; `recolour()` on the run and its sentences.
- [x] T005 The pure half: `linesOf`, `shownColour`, the palette's edits, the hex reader, `debounce`.
- [x] T006 `LineColours` on the project screen, and the design document's rule.
- [x] T007 Unit tests (colours, debounce, store, bridge, run) and the end-to-end scenarios.
- [x] T008 ARCHITECTURE, CLAUDE.md, spec 003's record contract.
- [x] T009 Lint, typecheck, unit and the build; the scanners; the reviewer.
- [ ] T010 The end-to-end suite (`tests/e2e/colours.spec.ts`), run serially by
      hand because three branches were in flight at once; then the pull request.
