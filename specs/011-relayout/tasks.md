# Tasks: Re-layout over the engine's addressed layouts

- [x] T001 Pin v0.5.0; the generator learns `prefixItems` and `maxLength`; regenerate.
- [x] T002 `layout` joins the error kinds; `LayoutDone` carries the id; the validators follow the engine's.
- [x] T003 `completeLayout` takes the id and reads nothing; `src/main/layout.ts` and its test go.
- [x] T004 The run passes the id to `map.build` and the record; `force` for a re-layout; the sentences.
- [x] T005 "Re-layout" behind the confirm dialog on the project screen.
- [x] T006 The stand-in engine stores layouts and refuses a map for one it has not.
- [x] T007 Unit tests (run, store, bridge, validators), the real-engine tests, the end-to-end re-layout and its cancel.
- [x] T008 ADR-033; ARCHITECTURE, CLAUDE.md, the rules, spec 007's contracts.
- [x] T009 Lint, typecheck, unit, build, end to end, the scanners, the reviewer; the pull request.
