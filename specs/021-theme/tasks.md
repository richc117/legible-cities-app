# Tasks: The theme a project's map is drawn in

- [x] T001 `isTheme` and `validateTheme` on the record; `parseRecord` uses them.
- [x] T002 `setTheme` in the API, the preload, the channels and the
      main-side handler; the store writes it.
- [x] T003 The viewer draws the project's theme, not the interface's.
- [x] T004 `ThemeSwitch` on the project screen, and the design document's rule.
- [x] T005 The stand-in engine echoes the theme `export.plan` was asked for.
- [x] T006 Unit tests (the record, the store, the bridge) and the
      end-to-end scenarios, the export among them.
- [x] T007 ARCHITECTURE, CLAUDE.md, spec 003's record contract.
- [x] T008 Lint, typecheck, unit and the build; the scanners; the reviewer;
      then the pull request.
